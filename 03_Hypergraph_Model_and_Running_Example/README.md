# Hypergraph Model and Running Example

Core deliverable: Hyper-Adjacency Tensor construction, symmetric tensor Laplacian, hub-vs-low-degree diffusion simulation (relocated here — see note below), RK4 tensor-PDE diffusion, full 3rd-order sparse tensor, tensor eigenvector centrality, SVD latent modes, SciPy reaction-diffusion PDE, robust multi-injection statistical validation.

**Requires:** run `02_Single_Patch_Fitting` first (or load its checkpoint).

**Source cells (original notebook):** 19–38 (reordered — see notebook for details)
